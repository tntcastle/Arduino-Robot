/*#ifndef LCD_DISPLAY_H
#define LCD_DISPLAY_H

#include <Arduino.h>
#include <LiquidCrystal.h>

// Declare the lcd object that will be used in both files
extern LiquidCrystal lcd;  // extern keyword allows us to use the lcd object across different files

// Declare the function that will be defined in the .cpp file
void show(String firstRow, String secondRow);

// voice commands
// speaker

#endif*/
